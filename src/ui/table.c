void draw_table(char*** data, int columns, int* lens, int* sizes) {
    for (int i = 0; i < columns; i++) {
        for (int j = 0; j < lens[i]; j++) {
            char row[] = "";
            for (int k = 0; k < sizes[i]; k++) {
                
            }
        }
    }
}